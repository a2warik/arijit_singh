From to add a song to publishing on web

python add_song.py # Helps to add a song in command line
python export_json.py # Helps in converting the database entries to json format and stores it in archive_data.json
python build_site.py # This help in refreshing the records in the html file

git add
git commit
git push

Now your data is available on the web

